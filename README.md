# Golden Meditation

## Install on iPhone

```sh
npm install
npm run ios:sync
npm run ios:open
```

In Xcode, select your connected iPhone, configure signing if prompted, then click Run. On the iPhone, trust the developer profile if iOS asks.

## Web development

This app uses React + Vite for the web layer.
